enum ButtonType { plane, gost, active, disable }

enum ButtonSize { fit, mini, small, medium, large }

enum DialogType { alert, confirm }

class Pairings {
  static const String all = '전체';
  static const String alcohol = '술';
  static const String foods = '안주';
}
